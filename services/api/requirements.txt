fastapi==0.115.6
uvicorn[standard]==0.34.0
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
geoalchemy2==0.16.0
alembic==1.14.0
pydantic==2.10.4
pydantic-settings==2.7.0
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
# GHIM CỨNG: passlib 1.7.4 không chạy được với bcrypt >= 5. Nó dò phiên bản qua
# bcrypt.__about__ (đã bị xoá ở 5.0) rồi rơi vào nhánh dự phòng, và nhánh đó ném
# 'password cannot be longer than 72 bytes' cho MỌI mật khẩu, kể cả mật khẩu ngắn.
# Bỏ ghim là đăng nhập nội bộ chết ngay lần cài lại tiếp theo.
bcrypt==4.2.1
pyotp==2.10.0
email-validator==2.2.0
python-multipart==0.0.20
redis==5.2.1
prometheus-client==0.26.0
celery[redis]==5.4.0
httpx==0.28.1
python-dotenv==1.0.1
pytest==8.3.4
pytest-asyncio==0.25.0
aiosqlite==0.20.0
