# Python
.venv/
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
*.egg-info/

# Node
node_modules/
dist/
build/

# Env & local data — chỉ bản .enc (đã mã hoá bằng SOPS) mới được vào repo
.env
.env.*
!.env.example
!.env.*.enc
# Khoá riêng age/SSH: không bao giờ commit
*.age
age.key
key.txt
*.db
*.sqlite3

# OS
.DS_Store

# Monorepo
.turbo/
.pnpm-store/
coverage/
*.log

# Sinh tự động - không commit
# (openapi.json thì CÓ commit: CI so bản sinh mới với bản này để phát hiện API đổi mà quên sinh lại client)
packages/api-client/src/generated/

# Coverage
.coverage
.coverage.*
htmlcov/

# Thư mục app Claude ghi file gửi cho người dùng
Claude outputs/
